# Hyprland keybindings for voxtype.
# Source this file from your hyprland.conf:
#   source = ~/.config/hypr/voxtype.bindings.conf

# Push-to-talk: hold F17 to record, release to transcribe and output via voxtype.
bind   = , F17, exec, voxtype record start
bindr  = , F17, exec, voxtype record stop
